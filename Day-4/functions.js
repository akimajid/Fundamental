// Do not
// Repeat
// Yourself

function sunTwoValues (firstValue, secondValue) {
    let result = firstValue + secondValue

    console. log(result)
}

// sunTwoValues (45, 90);
// sunTwoValues (99, 71);

function differentOFTwoValues (a, b) {
    let result = a - b

    console. log(result)
}

const a = 99
const b = 100

differentOFTwoValues (b, a)